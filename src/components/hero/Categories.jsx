import React, { useEffect } from 'react';

const Categories = () => {
    
    useEffect('.categories.json')
    .then(res => res.JSON())
    .
    return (
        <div>
            
        </div>
    );
};

export default Categories;